import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.corporatecheerleader.app',
  appName: 'Corporate Cheerleader',
  webDir: 'dist'
};

export default config;
